import { Component } from '@angular/core';

@Component({
   selector: 'page',
   template: `
    Page Component
   `
})
export class PageComponent  {
    // tslint:disable-next-line:no-empty
    constructor() {
    }
}
