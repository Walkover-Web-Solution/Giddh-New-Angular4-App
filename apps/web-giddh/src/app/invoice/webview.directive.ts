import { Directive } from '@angular/core';

/**
 * Handles Directive functionality
 */
@Directive({
    selector: 'webview',
    standalone: false
})

/** Dummy directive to allow html-tag 'webview' */
/**
 * WebviewDirective directive
 * Implements WebviewDirective functionality
 */
export class WebviewDirective { }
