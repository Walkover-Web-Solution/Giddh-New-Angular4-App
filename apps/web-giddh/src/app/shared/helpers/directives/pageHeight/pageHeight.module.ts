import { NgModule } from '@angular/core';
import { FullPageHeight } from './pageHeight.directive';

@NgModule({
    imports: [],
    exports: [FullPageHeight],
    declarations: [FullPageHeight],
})
export class PageHeightModule {
}
